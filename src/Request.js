
// Creates a klobb Request from a Node request.
//
// TODO: This is just stubbed out for proof of concept
//
// NodeRequest -> Request
export function fromNode(nreq) {
  return { 
    url: nreq.url 
  };
}
